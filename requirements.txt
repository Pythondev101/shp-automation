# Direct dependencies, pinned for reproducible runs.
# Browsers are installed separately: python -m playwright install chromium firefox webkit
pytest==9.1.1
playwright==1.62.0
pytest-playwright==0.9.0
python-dotenv==1.2.3
